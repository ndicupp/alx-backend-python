.github/
└── workflows/
    └── ci.yml

mkdir -p .github/workflows
touch .github/workflows/ci.yml

name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    # Setup a MySQL service for the tests to interact with
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: messaging_db_test
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests with Pytest
      env:
        # Pass database credentials to Django via environment variables
        DB_ENGINE: django.db.backends.mysql
        DB_NAME: messaging_db_test
        DB_USER: root
        DB_PASSWORD: root_password
        DB_HOST: 127.0.0.1
        DB_PORT: 3306
      run: |
        pytest


on:
  push:
  pull_request:

services:
  mysql:
    image: mysql:8.0

--health-cmd="mysqladmin ping"

env:
  DB_NAME: test_db
  DB_USER: test_user


uses: actions/checkout@v4

uses: actions/setup-python

pip install -r requirements.txt

python manage.py migrate

pytest

git add .github/workflows/ci.yml
git commit -m "Add GitHub Actions CI workflow for Django tests"
git push origin main


